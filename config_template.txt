# Database Configuration Template
# Copy this file to .env and fill in your database credentials
#
# Select backend: MongoDB is the only supported backend now
DB_BACKEND=mongodb

# MongoDB connection (required)
MONGO_URI=mongodb://localhost:27017
MONGO_DB=call-logs

# Instructions:
# 1. Copy this file and rename it to .env
# 2. Update the values above with your actual database credentials
# 3. Make sure .env is in .gitignore to keep credentials secure
